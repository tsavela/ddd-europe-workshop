namespace App.Commands
{
    public record SharePostCommand(
        /*
            Add here the necessary fields.
            HINT: A command should express an intention to change the system's state.
        */
    );
}